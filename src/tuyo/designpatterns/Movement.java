package tuyo.designpatterns;

public interface Movement { //Interface comum a todos estes algoritmos de: caminhar e correr.

	public void move(int distance); // Método move que recebe como parâmetro a distância de movimentação.
}
